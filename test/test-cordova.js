var should = require('should')

describe('cordova automation module', function() {

    describe('sanity', function() {
        it('you are sane', function(done) {
            true.should.eql(true)
            done()
        })
    })
    // end of tests
})
